% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reducekappa.R
\name{reduceKappa_wrapper}
\alias{reduceKappa_wrapper}
\title{Reduce and annotate pathway enrichment results}
\usage{
reduceKappa_wrapper(
  df,
  group_slice = NULL,
  geneset_id_col = "Geneset.ID",
  gene_col = "Genes.Returned",
  sig_col = "P.value",
  descrip_col = "Description",
  delim = NULL,
  rev_sig = FALSE,
  v = FALSE,
  filter_representative = FALSE
)
}
\arguments{
\item{df}{A data frame of pathway enrichment results. Must contain columns
identified by \code{geneset_id_col}, \code{gene_col}, \code{sig_col}, and \code{descrip_col}.}

\item{group_slice}{Character vector of column name(s). When comparing results
from multiple analyses, set this to the grouping column(s) to retain only
the most significant term per cluster per group. Default \code{NULL}.}

\item{geneset_id_col}{Name of the column containing unique geneset IDs.
Default \code{"Geneset.ID"}.}

\item{gene_col}{Name of the column containing genes associated with each
geneset, listed as a single character string with entries separated by
a space or punctuation mark (e.g. \code{", "} or \code{"/"}). Default \code{"Genes.Returned"}. \strong{Note:}
only genes associated with \emph{significant} genesets should be included as
supporting information to cluster pathways.}

\item{sig_col}{Name of the column used to select the representative term
within each cluster (smallest value = most significant). Default
\code{"P.value"}. See also \code{rev_sig}.}

\item{descrip_col}{Name of the column containing descriptive geneset names,
carried through to \code{cluster_term}. Default \code{"Description"}.}

\item{delim}{Delimiter separating genes in \code{gene_col}. If \code{NULL} (default),
the delimiter is auto-detected. Falls back to \code{", "} if detection fails.}

\item{rev_sig}{Logical. Set to \code{TRUE} if \code{sig_col} contains \code{-log10}
transformed p-values (larger = more significant). Values are back-transformed
before ranking. Default \code{FALSE}.}

\item{v}{Logical. Print progress messages. Default \code{FALSE}.}

\item{filter_representative}{Logical. If \code{TRUE}, retain only the single most
significant row per cluster (the representative term). Default \code{FALSE}.}
}
\value{
A data frame based on \code{df} with four additional columns prepended
before \code{geneset_id_col}:
\describe{
\item{\code{cluster}}{Integer cluster assignment.}
\item{\code{cluster_id}}{Geneset ID of the representative (most significant)
term in the cluster.}
\item{\code{cluster_term}}{Description of the representative term.}
\item{\code{cluster_size}}{Number of unique genesets in the cluster.}
}
The following attributes are attached to the returned data frame:
\describe{
\item{\code{n_og_terms}}{Number of unique genesets in the input.}
\item{\code{n_reduced_terms}}{Number of clusters (unique representative terms).}
\item{\code{genes_in_cluster_df}}{Data frame of all unique genes per cluster.}
\item{\code{cluster_info}}{Data frame with detailed cluster composition.}
}
}
\description{
Takes an enrichment results table and returns the same table annotated with
cluster information. Gene sets are clustered by kappa similarity
(see \code{\link[=reduceKappa]{reduceKappa()}}), and each cluster is named after the pathway with the
lowest p-value in that cluster. Additional summary information is attached
as attributes.
}
\examples{
library(dplyr)
data(pathway_res)

# Example 1: Reduce a single trial, keep only representative terms
reduced = pathway_res |>
  filter(data_label == "G", FDR < 0.05) |>
  reduceKappa_wrapper(filter_representative = TRUE)

attr(reduced, "n_og_terms")
attr(reduced, "n_reduced_terms")

# Example 2: Cross-trial clustering retaining one term per group per cluster
reduced_multi = pathway_res |>
  filter(FDR < 0.05) |>
  reduceKappa_wrapper(group_slice = "data_label")
}
