% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reducekappa.R
\name{reduceKappa}
\alias{reduceKappa}
\title{Cluster gene sets by kappa similarity}
\usage{
reduceKappa(df, collapse_small_clusters = FALSE)
}
\arguments{
\item{df}{A data frame whose \strong{first two columns} define the variables to
cluster and their characteristics. The first column should be the variable
to summarise (e.g. geneset ID) and the second column should be the
supporting feature (e.g. gene ID). Similarity between first-column entries
is computed based on overlap in second-column values.}

\item{collapse_small_clusters}{Logical. If \code{TRUE}, clusters with fewer than
2 members are merged into the nearest larger cluster based on average kappa
similarity. Use with caution. Default \code{FALSE}.}
}
\value{
A named integer vector: names are the unique values from the first
column of \code{df} and values are integer cluster assignments from hierarchical
clustering cut at \code{h = 0.7}.
}
\description{
Computes pairwise kappa similarity scores between gene sets based on shared
genes, then clusters them using hierarchical clustering. This implements the
approach described in the
\href{https://www.nature.com/articles/s41467-019-09234-6}{Metascape paper}.
}
\examples{
library(dplyr)
library(tidyr)
data(pathway_res)

# Build a long gene-per-row table for one trial
gene_long <- pathway_res |>
  filter(data_label == "G", FDR < 0.05) |>
  select(Geneset.ID, Genes.Returned) |>
  separate_longer_delim(Genes.Returned, delim = ", ")

clusters <- reduceKappa(gene_long)
head(clusters)
}
