% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reducekappa.R
\name{reduceKappa}
\alias{reduceKappa}
\title{Cluster gene sets by kappa similarity}
\usage{
reduceKappa(df, collapse_small_clusters = FALSE, hclust_cutoff = 0.7)
}
\arguments{
\item{df}{A two-column data frame with geneset IDs in the first column and
genes or proteins in the second column, with one gene per row. Cluster
similarity is calculated based on overlap in gene members across genesets.}

\item{collapse_small_clusters}{Logical. If \code{TRUE}, clusters with fewer than
2 members are merged into the nearest larger cluster based on average kappa
similarity. Use with caution. Default \code{FALSE}.}

\item{hclust_cutoff}{Numeric. Height at which to cut the hierarchical
clustering dendrogram to define clusters. Default \code{0.7}, as described in
the \href{https://www.nature.com/articles/s41467-019-09234-6}{Metascape paper}.}
}
\value{
A named integer vector: names are the unique values from the first
column of \code{df} and values are integer cluster assignments from hierarchical
clustering cut at \code{h = hclust_cutoff}.
}
\description{
Computes pairwise kappa similarity scores between gene sets based on shared
genes, then clusters them using hierarchical clustering. This implements the
approach described in the
\href{https://www.nature.com/articles/s41467-019-09234-6}{Metascape paper}.
}
\details{
This is the most flexible of the two functions — it returns cluster
assignments as a named vector that you can use however you like in your own
analysis. See \code{\link[=reduceKappa_wrapper]{reduceKappa_wrapper()}} for a higher-level function that takes
a full enrichment results table and returns it annotated with cluster
information.
}
\examples{
library(dplyr)
library(tidyr)
data(pathway_res)

# Build a long gene-per-row table for one trial
gene_long = pathway_res |>
  filter(data_label == "G", FDR < 0.05) |>
  select(Geneset.ID, Genes.Returned) |>
  separate_longer_delim(Genes.Returned, delim = ", ")

clusters = reduceKappa(gene_long)
head(clusters)
}
